const start = document.getElementById("source");
const end = document.getElementById("target");

let currentdraged = null;

const images = source.querySelectAll(".image");
images.foreaach(function(image){
    image.addeventlistener("dragstart", funstion(event)){
        e
    }
})